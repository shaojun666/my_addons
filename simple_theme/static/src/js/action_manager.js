odoo.define('custom_theme.ActionManager', function (require) {
    "use strict";

    var ActionManager = require('web.ActionManager');

    ActionManager.include({
        className: 'o_action_manager main',
        id:"main",

    });


});
